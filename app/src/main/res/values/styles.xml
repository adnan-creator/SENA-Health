<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:actionBarSize">75dp</item>
        <item name="actionBarSize">75dp</item>
        <item name="android:actionBarTabTextStyle">@style/AppTheme.ActionBar</item>
        <item name="actionBarTabTextStyle">@style/AppTheme.ActionBar</item>
        <!--Action bar style-->
<!--        <item name="android:actionBarStyle">@style/AppTheme.ActionBar</item>-->
<!--        <item name="actionBarStyle">@style/AppTheme.ActionBar</item>-->
    </style>

    <style name="AppTheme.ActionBar" parent="Widget.AppCompat.Light.ActionBar">
        <item name="android:titleTextStyle">@style/AppTheme.ActionBar.TitleText</item>
        <item name="titleTextStyle">@style/AppTheme.ActionBar.TitleText</item>
        <item name="android:height">75dp</item>
        <item name="height">75dp</item>
    </style>

    <style name="AppTheme.ActionBar.TitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textAlignment">center</item>
        <item name="android:fontFamily">sans-serif-condensed-light</item>
    </style>

    <style name="SeekBarWithoutSteps" parent="Base.Widget.AppCompat.SeekBar.Discrete">
        <item name="tickMark">@null</item>
    </style>

    <style name="ColoredHandleThemeWhite" parent="AppTheme">
        <item name="colorControlActivated">@color/white</item>
    </style>
</resources>